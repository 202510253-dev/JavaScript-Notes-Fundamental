// Declaration and Strict MOde = In strict mode, you can't use undeclared variables or functions.

"use strict"; // This is a statement that tells the interpreter to use strict mode.

height = 100;
console.log(height);
// repeat() = A method is a built-in function that allows you to repeat a string a specified number of times

// string.repeat(times) = Basic Syntax

let word = "Hello! ";
let repeatedWord = word.repeat(3);
console.log(repeatedWord);

// count = A parameter that bust be a non-negative number. If you pass a negative number, JS will throw a RangeError
